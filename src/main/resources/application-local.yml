spring:
    datasource:
        url: ${DB_URL:jdbc:oracle:thin:@175.126.145.185:1521:orcl}
        username: ${DB_USERNAME:aibe5team4}
        password: ${DB_PASSWORD:akstp}
        driver-class-name: oracle.jdbc.OracleDriver

    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.community.dialect.OracleLegacyDialect
                format_sql: true
                jdbc:
                    time_zone: Asia/Seoul

logging:
    level:
        com.ieum.ansimdonghaeng: debug
        org.hibernate.SQL: debug
        org.hibernate.orm.jdbc.bind: trace

app:
    jwt:
        secret: ansimdonghaeng-local-jwt-secret-key-2026
    file-storage:
        base-dir: ${FILE_STORAGE_BASE_DIR:./storage/local}
