spring:
  application:
    name: ansimdonghaeng-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  jackson:
    time-zone: Asia/Seoul
    serialization:
      write-dates-as-timestamps: false
  jpa:
    open-in-view: false

server:
  port: ${SERVER_PORT:8080}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  packages-to-scan: com.ieum.ansimdonghaeng

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

app:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-me-123456}
    issuer: ${JWT_ISSUER:ansimdonghaeng}
    access-token-expiration-minutes: ${JWT_ACCESS_TOKEN_EXPIRATION_MINUTES:60}
    header: Authorization
    prefix: Bearer
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
  websocket:
    endpoint: ${WEBSOCKET_ENDPOINT:/ws}
  ai:
    base-url: ${AI_BASE_URL:https://example.invalid}
